package main

//func parseAndSaveData(inputSource, outputSource) error

func main() {
	// We were assigned to a design & develop task: "Parse data from many sources then save data to
	// many storage types". How we design it to allow many developers can help build it faster.
}
